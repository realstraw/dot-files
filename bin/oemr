#!/usr/bin/env bash

## currently for mac only

usage="USAGE: $0 <emr-job-id>"

if [ $# -ne 1 ]; then
    echo $usage
    exit 1
fi

emr_id=$1; shift

open "https://console.aws.amazon.com/elasticmapreduce/home?region${AWS_DEFAULT_REGION}#cluster-details:${emr_id}"
