# ignore my virtualenv files
--ignore-dir=.ENV

# ignore scala target files
--ignore-dir=target

# ignore python build files
--ignore-dir=build

# ignore ctags file
--ignore-file=is:.tags

# ignore idea files
--ignore-dir=.idea

# ignore eclipse IDE stuff
--ignore-dir=.cache

# ignore metals
--ignore-dir=.metals

# ignore .bloop for metals
--ignore-dir=.bloop

# ignore terraform files
--ignore-dir=.terraform

# ignore node lib dir
--ignore-dir=lib

# ignore node module dir
--ignore-dir=node_modules

# ignore package lock
--ignore-file=is:package-lock.json

# ignore typescript build info
--ignore-file=ext:tsbuildinfo
